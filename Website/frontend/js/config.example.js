// Example Configuration - Rename this file to config.js
const config = {
    // Default language (e.g., 'de', 'en')
    language: 'en',
    // API Base URL (example)
    apiBaseUrl: 'http://example.com/api'
};